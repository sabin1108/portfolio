const {chromium}=require('playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch();try{const p=await b.newPage({viewport:{width:1440,height:1000}});for(const route of ['/','/portfolio_bin','/past','/frontend']){await p.goto('http://localhost:5173'+route);await p.waitForLoadState('networkidle');if(route==='/past'||route==='/frontend'){assert.equal(await p.locator('.portfolio-page').count(),1);assert.equal(await p.locator('.bin-deck').count(),0);}else{assert.equal(await p.locator('.bin-deck').count(),2);assert.equal(await p.locator('.bin-work-footer a[href="/past"]').count(),1);}console.log('PASS route '+route);}}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});