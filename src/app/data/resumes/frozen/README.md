# Frozen Resume Data

수정 금지 이력서 보관 영역입니다.

현재 수정 가능한 이력서 파일은 상위 `src/app/data/resumes` 폴더의 아래 파일뿐입니다.

- `resume_manual.ts`
- `resume_SysmasterDB.ts`
- `resume_wacus.ts`
- `resume_ax.ts`

이 폴더 안 파일은 이미 제출/완료된 회사별 이력서입니다. 새 회사 이력서는 `resume_manual.ts`를 복사해서 만들고, 기존 완료본을 직접 수정하지 않습니다.